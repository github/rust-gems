[package]
name = "casefold-benchmarks"
edition = "2021"

[lib]
path = "lib.rs"
test = false

[[bench]]
name = "performance"
path = "performance.rs"
harness = false
test = false

[dependencies]
casefold = { path = ".." }
criterion = "0.8"
foldhash = "0.1"
rand = "0.10"
